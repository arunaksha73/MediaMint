{
  "name": "mediamint-downloader",
  "version": "1.0.0",
  "description": "MediaMint — Instagram Reels & Video Downloader",
  "main": "backend/server.js",
  "scripts": {
    "postinstall": "cd backend && npm install && (pip install yt-dlp || python3 -m pip install yt-dlp || true)",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "helmet": "^7.1.0",
    "node-fetch": "^2.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
